// find the longest subarray sum
#include <bits/stdc++.h>
using namespace std;

int maxSum(vector<int> &nums)
{
    for (int i = 0; i < nums.size(); i++)
    {
        vector<int> ans;
        for (int j = i; j < nums.size(); j++)
        {
            ans.push_back(nums[i]);
        }
        
    }
    
}

int main()
{
    
    return 0;
}